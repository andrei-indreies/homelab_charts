apiVersion: v2
name: headscale
description: Headscale VPN control plane with Headplane web UI
type: application

# Chart version — increment on every chart change
version: 0.1.1

# Application version — tracks the Headscale container image tag
appVersion: "0.29.0"

keywords:
  - headscale
  - tailscale
  - vpn
  - wireguard
  - headplane

maintainers:
  - name: Andrei Indreies

dependencies: []
