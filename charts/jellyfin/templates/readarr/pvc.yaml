{{- if and .Values.readarr.persistence.config.enabled (not .Values.readarr.persistence.config.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config
  labels:
    {{- include "jellyfin.labels" . | nindent 4 }}
  {{- with .Values.readarr.persistence.config.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.readarr.persistence.config.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.readarr.persistence.config.size | quote }}
  {{- with .Values.readarr.persistence.config.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}