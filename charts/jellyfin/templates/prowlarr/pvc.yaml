{{- if and .Values.prowlarr.persistence.config.enabled (not .Values.prowlarr.persistence.config.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config
  labels:
    {{- include "jellyfin.labels" . | nindent 4 }}
  {{- with .Values.prowlarr.persistence.config.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.prowlarr.persistence.config.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.prowlarr.persistence.config.size | quote }}
  {{- with .Values.prowlarr.persistence.config.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}
