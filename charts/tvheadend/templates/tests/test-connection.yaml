apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tvheadend.fullname" . }}-test-connection"
  labels:
    {{- include "tvheadend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tvheadend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
